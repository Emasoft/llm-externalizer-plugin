[project]
name = "llm-externalizer-plugin"
version = "3.5.2"
description = "Claude Code plugin: offload bounded LLM tasks to cheaper local or remote models via MCP"
requires-python = ">=3.12"

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
