---
trdd-id: 63314265-0db7-4ca2-9765-016ce0d53a7b
title: Phased split of the index.ts monolith (B1) — boundaries, deps, order
column: complete
created: 2026-06-20T19:50:53+0200
updated: 2026-08-19T10:05:00+0200
current-owner: go-on-yourself
assignee: null
priority: 4
severity: MEDIUM
effort: XL
task-type: refactor
parent-trdd: TRDD-828238b5
relevant-rules: []
release-via: publish
delivery: direct-push
target-branch: main
test-requirements: [unit, lint, typecheck]
impacts: [public-api]
---

# TRDD-63314265 — Phased split of the index.ts monolith (B1)

**Filename:** `design/tasks/TRDD-20260620_195053+0200-63314265-index-monolith-split-plan.md`
**Tracked in:** this repo (design/tasks/ is git-tracked)
**Parent:** TRDD-828238b5 Part B / B1 (this is the dedicated B1 plan it asked for).

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-20

**COMPLETE 2026-08-19 — every item below is DONE, including P4b.** The
"deferred as marginal" P4b cluster (`FREE_FLOOR_MIN_CONTEXT_TOKENS`,
`filterFreeModels`, `selectFreeEnsembleModels`) was in fact extracted to
`free-rotation.ts` during the v10.3.x free-rotation work (it survived the
mcp-server→scripts/llm-ext migration in 5dc5c58); index.ts imports it and
`free-rotation.test.ts` covers it. index.ts stands at 6251 lines with the
provider/completion/dispatch/tool-def layers all extracted. The NEXT-ACTION
text below (P4b, A6 pointers) is HISTORICAL — A6 lives with its parent
TRDD-828238b5 (backburner; needs user-curated golden datasets).

- **What this is:** the phased extraction PLAN for splitting `mcp-server/src/index.ts`
  (8457 lines as of 2026-06-20; the TRDD-828238b5 "9599" figure is STALE — the A6
  search-existing extraction already shrank it). B1 is the last structural item in
  the 828238b5 backlog; A6 (free-form benchmarks) is BLOCKED on Phase 3 below.
- **Why a PLAN and not a done refactor:** a deep investigation (2026-06-20)
  established that index.ts has NO clean, co-located, low-coupling block for a
  quick safe extraction — every candidate is either (a) tiny + scattered + interleaved
  with impure module state, or (b) a large block heavily coupled to shared
  module-level state (the rate limiter, backend config, fetch helpers, the
  auto-free flags, the catalog cache). The global directives mandate phased
  execution with verification (and approval gates) for a multi-file refactor of
  SHIPPED code; rushing entangled extractions overnight on v10.0.0 risks breaking
  the server for modularity gains. So the deliverable now is this plan.
- **PHASE 5a DONE (2026-07-11) — the provider/transport layer.** Extracted to a new
  `src/provider/` package, all importing ZERO from index.ts:
  `provider/types.ts` (BackendConfig, ConnectionSetup, ChatMessage, StreamingResult,
  ModelInfo, VALID_REASONING_EFFORTS/ReasoningEffortSetting + the **`ProviderDeps`
  seam**), `provider/http.ts` (`fetchWithTimeout`, `fetchWithRetry429`,
  `computeBackoffMs` — stateless, zero deps), `provider/lmstudio.ts` (the whole LM
  Studio native block: the per-endpoint probe cache, `detectLMStudio`,
  `chatCompletionNative`), `provider/connection.ts` (`resolveConnection`).
  index.ts **6685 → 6190 (−495)**; it now builds ONE module-level `providerDeps`
  object and passes it at the 5 call sites. **Every ProviderDeps field that reads
  mutable state is a FUNCTION** (`getBackend`, `apiHeaders`, `getSoftTimeoutMs`,
  `isFreeOnly`, `isLMStudioProvider`) — a captured VALUE would pin the provider layer
  to the pre-reload backend generation forever (settings reload reassigns
  `currentBackend`/`activeResolved`/`SOFT_TIMEOUT_MS`). `assertFreeOnlyModel` is
  imported into connection.ts straight from config.ts (config.ts never imports
  index.ts, by design — no cycle). Dropped one stale orphan docstring ("Read from a
  stream with a per-chunk timeout") for a stream reader that no longer exists, and one
  duplicated `fetchWithRetry429` docstring. Gates: build + lint (`eslint src
  --max-warnings 0`) green, full suite **1247 pass / 0 fail**, `doc-consistency` (11)
  + `index` roster (23) green ⇒ ZERO tool drift. dist rebuilt + committed.
- **PHASE 5b DONE (2026-07-11) — the completion + retry layer.** `commit ee13b2f`.
  New `src/provider/completion.ts` (1090 lines, imports ZERO from index.ts):
  `chatCompletionSimple`, `chatCompletionJSON`, `chatCompletionWithRetry`, plus the
  FOUR state clusters that only they ever touched — the reasoning ladder
  (`MODEL_REASONING_CACHE` + `DEFAULT_REASONING_EFFORT` + `reasoningLadderForModel`
  + `recordReasoningRejection` + `isReasoningRejectionError`), the
  supported_parameters filter (`MODEL_SUPPORTED_PARAMS` + `FILTERABLE_REQUEST_FIELDS`
  + `FILTER_WARN_SEEN` + `getModelSupportedParams` + `filterBodyForSupportedParams`),
  the `LLM_EXT_DUMP_REQUESTS` hook (`dumpRequestBody`), and the SERVICE_HEALTH circuit
  breaker (`recordServiceSuccess/Failure` + `checkServiceHealthOrWait`). Also moved:
  `withSystemCacheBreakpoint`, `EXTRACT_PATHS_SCHEMA`, `JSONCompletionResult`; and
  `sanitizeProviderError` → `provider/http.ts` (stateless transform of an HTTP body).
  index.ts **6190 → 5147 (−1043)**.
  **THE LOAD-BEARING DECISION — what could move vs what could NOT.** Each of the four
  caches was verified (grep of every reference) to have ZERO readers/writers outside
  the three completion functions, so moving them keeps each one a SINGLE binding with
  ONE owning module. `creditExhausted` and the auto-free flags are the OPPOSITE:
  index.ts reads them elsewhere (`shouldUseFree`, the dispatch layer,
  `getEnsembleModels`), so they STAY in index.ts and completion.ts writes them THROUGH
  the seam (`setCreditExhausted()` / `engageAutoFree()` / `invalidateBalanceCache()`).
  A local copy in completion.ts would diverge from what index.ts reads on the next
  call — the session would keep SPENDING after a 402. Comments at both ends say so.
  **Seam:** P5a's `ProviderDeps` REUSED, extended to `CompletionDeps` (+5 fields:
  `getDefaultMaxTokens`, `getFreeModelId`, `setCreditExhausted`, `engageAutoFree`,
  `invalidateBalanceCache`). Every field is a FUNCTION — for P5a's settings-reload
  reason AND because `FREE_MODEL_ID` is a `const` declared LATER in index.ts than the
  deps object, so an eager read throws on TDZ at module init.
  **The 5a report's "~50 call sites" was WRONG** — the real count is 10
  (chatCompletionSimple ×2, both INSIDE chatCompletionWithRetry so they moved with it;
  chatCompletionJSON ×2; chatCompletionWithRetry ×6). Every other "hit" was a comment.
  That is why 5b turned out to be a clean single cut, not a churn phase.
  No re-export shims (project rule): `reasoning-ladder.test.ts`,
  `provider-error-sanitize.test.ts`, `high-quality-scan.test.ts` now import from the
  new homes. Gates: build + `eslint src --max-warnings 0` green, full suite
  **1247 pass / 0 fail** (75 files), `doc-consistency` (11) + `index` roster (23) green
  ⇒ ZERO tool drift. dist rebuilt + committed (`dist/cli.js` + `dist/benchmark.js`
  came out byte-identical).
  **WHAT REMAINS:** **P4b** (`selectFreeEnsembleModels` + `FREE_FLOOR_MIN_CONTEXT_TOKENS`
  + `filterFreeModels` + `isModelUnavailableError`, ~80 lines) — still DEFERRED as
  marginal (see below; zero coverage gain, 2 test files to repoint). There is no
  Phase 5c: the provider/completion layer is now fully extracted. The next structural
  candidate, if any, is the `dispatchCallToolInner` tail and the report/formatting
  helpers — NOT yet planned, and index.ts at 5147 is no longer the outlier it was
  at 8457.
- **NEXT ACTION:** Phases 1, 2, 2b, 4 DONE + **Phase 3 DONE (both tools)** + **Phase 5
  DONE (5a + 5b — the whole provider layer)**. The ONLY remaining item in this TRDD is
  **P4b**, still deliberately deferred as marginal. The next real priority is **A6**
  (free-form `code_task`/`scan_folder` benchmarks — needs REAL hand-curated golden
  datasets + an LLM-judge scorer; do NOT fabricate datasets). Historical detail below.
  Extracted:
  `request-overrides.ts` (P1) + `rate-limiter.ts` (P2/P2b) + `tools/definitions.ts` (P4)
  + `scan-folder/core.ts` (`runScanFolder`, case 321→21) + `code-task/core.ts`
  (`runCodeTask`, case 1091→~26; single + BOTH batch modes; 14-seam `CodeTaskDeps`).
  **index.ts is now 6587 lines (from 8457 at session start — −1870, ~22% smaller).**
  Full suite **1196 pass** + build + lint green; `doc-consistency` + `index` roster confirm
  ZERO tool drift. Both dispatch cores import ZERO from index.ts (load without `main()`) and
  are hermetically tested (fake LLM seam, real pipeline) — **A6 is now UNBLOCKED**.
  **NEXT (priority order):** (1) **A6** — free-form `code_task`/`scan_folder` benchmarks can
  now import `runCodeTask`/`runScanFolder`; still needs REAL hand-curated golden datasets +
  an LLM-judge scorer (the hard no-fakes part — best with the user / careful dataset work,
  do NOT fabricate datasets). (2) **Phase 5** — completion/connection layers (1939-2435 etc.)
  + the deferred **P4b** ensemble helpers (selectFreeEnsembleModels,
  FREE_FLOOR_MIN_CONTEXT_TOKENS). The two giant blocks the section map flagged (tool defs +
  dispatch) are now BOTH broken up — the highest-value monolith reduction is done.
- **P4b free-ensemble extraction — INVESTIGATED 2026-06-20, DEFERRED (marginal).**
  The cluster (`FREE_FLOOR_MIN_CONTEXT_TOKENS`, `filterFreeModels`,
  `selectFreeEnsembleModels`, `isModelUnavailableError`, index.ts ~3474-3554) is
  PURE but already FULLY unit-tested (`free-only.test.ts` + `auto-free.test.ts`
  have dedicated describe blocks importing all four) AND index.ts is already
  testable via its entry-point guard — so extracting it adds ZERO coverage and
  only ~80 lines off index.ts, at the cost of repointing 2 test files' multi-line
  imports. Per "only what is strictly necessary", not worth churning shipped
  v10.0.0. The investigation DID fix a real doc-drift bug: `getEnsembleModels`'
  orphaned `/** Build ensemble model list */` docstring (stranded ~140 lines from
  its function) restored to it — **commit d77cf60**, source-only (dist
  byte-identical; esbuild strips JSDoc).
- **Load-bearing facts/gotchas:**
  - `index.ts`'s `main()` is ENTRY-GUARDED (`if (__isEntrypoint) main()` at ~6583,
    `__isEntrypoint = realpathSync(entry) === import.meta.url`), so a plain `import`
    does NOT run `main()`/spawn the server — that is how `free-only.test.ts` +
    `auto-free.test.ts` test index.ts internals directly. (The earlier "runs main()
    on import" was IMPRECISE — CORRECTED 2026-06-20.) Importing index.ts is still
    HEAVYWEIGHT (runs all module-init + its whole dep graph), so extracted dispatch
    CORES still avoid importing it — for isolation + load-weight, not because main()
    would run.
  - The 1133-test TS suite + `doc-consistency` gate is the regression guard.
  - `dist/` is tracked → every phase must `npm run build` and commit the rebuilt
    bundle alongside the source.
  - When MOVING a symbol out and importing it back, index.ts internal call sites
    resolve to the imported binding automatically — but ALL OTHER importers
    (`grep -rl "<sym>" --include=*.ts | grep -v index.ts`) and test files must be
    updated, and the moved module must NOT import back from index.ts (circular).
  - NO backward-compat re-export shims (project rule) — update importers instead.
  - **PATH UPDATE 2026-08-06:** the target file moved in the v11.0.0 MCP→CLI migration — it is now `scripts/llm-ext/src/index.ts` (5371 lines), not `mcp-server/src/index.ts`.

## Section map (index.ts, line ranges as of 2026-06-20)

| Lines | Section | ~LOC | Coupling |
|---|---|---|---|
| 62-111 | File reading / grouping / scanning helpers | 49 | low (much already in scan-pipeline.ts) |
| 276-354 | Reasoning effort cache + default | 78 | med (MODEL_REASONING_CACHE state) |
| 354-407 | Per-model request body overrides (`applyModelOverrides`) | 53 | **PURE** |
| 408-561 | supported_parameters filter + reasoning-rejection | 154 | med (backend, fetch, appendModelEvent) |
| 825-963 | OpenRouter rate-limit detection | 139 | med |
| 967-1195 | session accounting + balance + auto-free state machine | 228 | **high** (mutable session/balance/auto-free state) |
| 1386-1614 | Connection setup (universal) | 229 | high |
| 1664-1908 | LM Studio native API | 245 | med |
| 1939-2435 | Non-streaming text + JSON completion | 497 | high |
| 2622-2676 | Error classification (`classifyError`) | 55 | high (mutates creditExhausted, engageAutoFree, rate limiter) |
| 2677-2775 | Adaptive rate limiter (AIMD) | 99 | **low-med** (self-contained class) |
| 2776-2834 | Rate-limited parallel executor | 59 | med |
| 3092-3334 | Retry-on-truncation wrapper | 243 | high |
| 3335-3501 | Ensemble streaming helper | 167 | high |
| 3588-4919 | **MCP Tool definitions (`buildTools`)** | **1332** | med (mostly static schema + a few dynamic desc helpers) |
| 5034-8354 | **Registered tools registry (the dispatch handlers)** | **3321** | **very high** (every tool's core logic + all shared state) |

The two giant blocks (tool defs 1332, dispatch 3321) are ~55% of the file and are
the only extractions that meaningfully "split the monolith". Both are the
highest-coupling. The dispatch block is where `code_task` / `scan_folder` live.

## Phase plan (each phase: ≤5 files, build+test+lint after, commit, then review)

- **Phase 1 — pure leaves (lowest risk). [DONE 2026-06-20 — `request-overrides.ts`]** Extract the genuinely-pure helpers to
  `request-overrides.ts` (`MODEL_REQUEST_OVERRIDES` + `applyModelOverrides`, 354-407)
  and `auto-free.ts` (the 4 pure helpers `parseFreeBelowUsd`, `resolveFreeModelId`,
  `resolveAutoFreePool`, `resolveSubsystemFreeModel`; LEAVE `selectFreeEnsembleModels`
  + `FREE_FLOOR_MIN_CONTEXT_TOKENS` for Phase 4 — they sit at 3819/3858 coupled to
  ensemble/benchmark state). Update importers: `auto-free.test.ts`, `free-only.test.ts`
  (point at the new module for the moved symbols). Guard: the existing tests.
- **Phase 2 — self-contained class + whole rate-limiting module. [DONE 2026-06-20 — `rate-limiter.ts`; Phase 2b folded the rest in]** Phase 2 extracted the AIMD
  `AdaptiveRateLimiter` class. **Phase 2b** then moved the shared singleton +
  `getAdaptiveRateLimiter()` factory + `signalSuccess()`/`signalRateLimitHit()`
  accessors + the `rateLimitedParallel()` executor + the `ProgressFn` type +
  `HEARTBEAT_INTERVAL_MS` into `rate-limiter.ts` — index.ts now holds ZERO
  rate-limiting state and signals the shared singleton via the two accessors from
  `classifyError` + the completion site. +21 tests total (deterministic AIMD class +
  executor order/concurrency-cap/progress/no-throw-accessors). Singleton sharing is
  preserved (ESM module = single binding).
- **Phase 3 — the dispatch cores (UNBLOCKS A6, highest risk). [DONE 2026-06-20 — both tools; A6 UNBLOCKED]** Extract the
  `code_task` and `scan_folder` handler cores from the dispatch block into
  importable modules (mirror the A6 `runSearchExistingImplementations` extraction:
  inject the shared seams — fetch impl, model resolution, rate limiter — rather than
  reading module globals, so the core imports WITHOUT triggering `main()`). This is
  the gate for A6 (TRDD-828238b5 A6 free-form benchmarks). Do it in its own sub-phases
  per tool, each guarded by the full suite + a new in-process runner test.
  **Scoped 2026-06-20** (current line ranges; `switch (name)` dispatch at index.ts:3919):
  `case "code_task"` = **4221-5312 (~1091 lines)**; `case "scan_folder"` = **5313-5634
  (~321 lines)**; the REFERENCE is `case "search_existing_implementations"` (5635) — already
  thin, delegating to `runSearchExistingImplementations` in `search-existing/core.ts`, with
  its hermetic test `benchmark/search-existing/runner.test.ts` (fake FetchImpl seam, real
  pipeline + scorer). Mirror that EXACT shape per tool: extract `runCodeTask(opts, {fetchImpl,
  …seams})` + `runScanFolder(...)` into `code-task/core.ts` + `scan-folder/core.ts`, make each
  `case` thin, add a hermetic runner test each. Do code_task and scan_folder as SEPARATE
  sub-phases (each its own commit + verify). This is a DESIGN+extraction (NOT a mechanical
  move like P4) — read each handler fully + enumerate EVERY shared seam (fetch, model
  resolution, rate limiter, output dir, config, redaction) BEFORE cutting; that design step
  needs fresh context, so it is the next burst's first task.
  **scan_folder DONE 2026-06-20** — `runScanFolder(args, ScanFolderDeps)` in
  `scan-folder/core.ts` (ZERO index.ts import; imports only scan-pipeline/grouping/
  rate-limiter + node builtins). `ScanFolderDeps` mirrors SeiDeps but uses
  `processFileCheck` (the per-file seam) instead of `callModel`, adds `getRateLimitConfig`
  / `resolveDefaultMaxTokens` / `modelOverride`, omits the unused `ensembleModelLabel`.
  Case 321→21 lines; `benchmark/scan-folder/runner.test.ts` = 5 hermetic tests (fake
  processFileCheck, real rateLimitedParallel + report assembly). Build + full suite 1190
  + lint green. `code_task` is next, same pattern (it is bigger: single + batch modes).
  **code_task DONE 2026-06-20** — `runCodeTask(args, CodeTaskDeps)` in `code-task/core.ts`
  (ZERO index.ts import; case 1091→~26 lines). `CodeTaskDeps` (14 seams) extends the
  ScanFolderDeps shape with code_task's extras: `ensembleStreaming` (multi-model inline+batch
  seam), `formatFooter`, `robustPerFileProcess` (mode-0 max_retries>1 parallel+retry+breaker
  path), `codeTaskSystemPrompt`, `ensembleModelLabel`, `normalizePaths`, `resolveFolderPath`,
  `defaultTemperature`; omits `backendModel`/`classifyError`/`getRateLimitConfig` (only used
  inside injected helpers). `benchmark/code-task/runner.test.ts` = 6 hermetic tests (single +
  inline + batch-mode2 + batch-mode0-sequential + batch-mode0-robust + validation-error).
  Build + full suite 1196 + lint green. **Phase 3 COMPLETE → A6 is UNBLOCKED.**
- **Phase 4 — tool definitions. [DONE 2026-06-20 — `tools/definitions.ts`]** Moved `buildTools()` (was ~3811-4730,
  919 lines) + its 5 EXCLUSIVE schema helpers (BATCHING_NOTE, answerModeSchema,
  maxRetriesSchema, folderSchemaProps, redactRegexSchema) into `tools/definitions.ts`,
  with `limitsBlock()` injected as a `limitsText` param (8 sites) so the dynamic
  description refresh (`refreshAllToolDescriptions` + `limitsBlock`, both KEPT in
  index.ts) still works. `doc-inventory.ts::readCoreToolNames` repointed to the new
  file (6-space-indent regex unchanged). The `doc-consistency` + `index` roster gates
  confirmed ZERO tool-count/name/description drift; full suite 1185 pass. STILL PENDING
  (deferred — these are NOT buildTools deps, so they did not move): the ensemble/free
  helpers `selectFreeEnsembleModels` + `FREE_FLOOR_MIN_CONTEXT_TOKENS` (~3819/3858,
  coupled to ensemble/benchmark state) — fold into Phase 5 or a P4b.
- **Phase 5a — connection + transport + LM Studio. [DONE 2026-07-11 — `src/provider/`]**
  `provider/{types,http,lmstudio,connection}.ts` + the `ProviderDeps` seam; index.ts
  6685 → 6190 (−495). Details + gotchas in the STATE block above.
- **Phase 5b — the completion trio (NOT done).** `chatCompletionSimple`,
  `chatCompletionJSON`, `chatCompletionWithRetry` (retry-on-truncation). Highest
  coupling left in the file: auto-free/credit state machine, session accounting,
  `classifyError`, reasoning-effort cache + rejection ladder, model events, rate-limiter
  signals — and `chatCompletionSimple` has ~50 call sites, so the deps object must be
  designed (and the call sites threaded) as its own phase. Do NOT fold it into another
  phase.

## Risk + acceptance

- Acceptance per phase: `npm run build` (tsc+esbuild) green, `npm test` (≥1133) green,
  `eslint --max-warnings 0` clean, `doc-consistency` green, dist rebuilt + committed.
- Roll back a phase with `git revert` if the suite goes red — never force a partial.
- Do NOT combine phases. Do NOT introduce re-export shims. Do NOT let an extracted
  module import back from index.ts.

## Why deferred from the 2026-06-20 autonomous session

Done this session under "complete all pending tasks": B3 (memory guard), Part E
(reconciled — was already resolved; added the F7 regression test), Part F (97 tests).
B1 was investigated to this plan but NOT executed: the directives gate a multi-file
refactor of shipped code behind phased execution + review, and no clean quick win
exists. Phase 1 is ready to run on the next pass.

## Approval log

- 2026-08-19T10:05:00+0200 — COMPLETE. Verified first-hand against the working
  tree: phases 1, 2, 2b, 3, 4, 5a, 5b all recorded DONE in STATE with their
  commits, and the sole open remainder (P4b) turned out already shipped —
  the cluster lives in `free-rotation.ts` with its own test file, imported by
  index.ts (grep-verified 2026-08-19). All extraction code long since
  published (latest release v13.5.9). Card had gone stale in `todo`, same
  stale-column pattern as TRDD-Q185FRMW.
